os: linux
sudo: required
language: "minimal"
services:
- docker

jobs:
  include:
  - name: "compile"
    script:
    - docker run -t -v $(pwd):/opt/builder/ --entrypoint ./scripts/compile.sh shesta/bootstrapcpp

notifications:
    email: false
