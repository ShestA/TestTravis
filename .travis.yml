os: linux
sudo: required
language: "minimal"
services:
- docker

jobs:
  include:
  - name: "quality"
    script:
    - docker run -t -v $(pwd):/opt/builder/ --entrypoint ./scripts/duplication.sh shesta/testtravis
  - name: "compile"
    script:
    - docker run -t -v $(pwd):/opt/builder/ --entrypoint ./scripts/compile.sh shesta/testtravis

notifications:
    email: false
